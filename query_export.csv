query,dialect,timestamp
"SELECT *
FROM employees
WHERE age > 50;
```

This assumes you have a table named `employees` with at least an `age` column.  If your table has a different name or the age column is named differently, adjust the query according",mysql,2025-03-08T16:41:11.370502
